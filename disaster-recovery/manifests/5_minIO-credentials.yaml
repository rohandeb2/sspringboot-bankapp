apiVersion: v1
kind: Secret
metadata:
  name: velero-minio-secret
  namespace: velero

type: Opaque

stringData:
  cloud: |
    [default]
    aws_access_key_id = rohan-admin
    aws_secret_access_key = secure-storage-pass