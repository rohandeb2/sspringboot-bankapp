apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "banking.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "banking.labels" . | nindent 4 }}
data:
  # --- 12 LPA LOGIC: ENVIRONMENT ABSTRACTION ---
  # These values are pulled dynamically from your values.yaml
  SPRING_PROFILES_ACTIVE: {{ .Values.config.springProfile | default "prod" | quote }}
  LOG_LEVEL: {{ .Values.config.logLevel | default "INFO" | quote }}
  
  # Database Host (Points to the AWS RDS Endpoint)
  DB_HOST: {{ .Values.config.dbHost | quote }}
  DB_PORT: {{ .Values.config.dbPort | default "3306" | quote }}
  DB_NAME: {{ .Values.config.dbName | quote }}

  # Custom Application Properties (Example: Feature Toggling)
  ENABLE_TRANSACTION_LIMITS: "true"
  MAX_DAILY_TRANSFER: "50000"